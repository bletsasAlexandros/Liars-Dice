import React, {useState, useRef, useEffect, FunctionComponent} from 'react';

interface PlayerProps{
    
}

export const Player:React.FC<PlayerProps> = (props:any)=>{
    return(
        <div>Hi</div>
    )
}